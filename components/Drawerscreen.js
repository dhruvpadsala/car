import React from 'react'
import { View, Text } from 'react-native'

export default function Drawerscreen({navigation}) {
    return (
      <View>
        <Text style={{fontSize: 25, color: 'white',backgroundColor:'red'}}>Good Morning Dhruv</Text>
      </View>
    );
}
